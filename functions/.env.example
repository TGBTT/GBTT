# Apps Script web app URL — same value as VITE_FORM_ENDPOINT in the client apps
FORM_ENDPOINT=https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec

# Shared secret — must match FUNCTIONS_WEBHOOK_SECRET in Apps Script Script properties
FUNCTIONS_WEBHOOK_SECRET=
